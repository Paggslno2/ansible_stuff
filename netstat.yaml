---
 - name: Ansible win_command module example  
   hosts: all    
   tasks: 
   -  name: run an executable command on a remote Windows system
      win_command: netstat -an # win_command is a Windows module. 

